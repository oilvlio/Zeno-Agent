## 修复

- 探测结果 spool 的磁盘剩余空间门槛默认 512MiB 降至 64MiB。一轮探测结果只有几 KB JSON，而 spool 自身上限才 256MiB 待发 + 16MiB 隔离，旧门槛会让小磁盘机器（剩余空间不足 512MiB 时）丢掉全部 ping 上报——机器看着在线，延迟数据全无。现 64MiB 仍保留充足余量。

## 验证

- `internal/agent` 探针 spool 单测（默认限额与低磁盘拒收）同步更新。
- 全量测试、race、静态检查及发布流水线通过。
